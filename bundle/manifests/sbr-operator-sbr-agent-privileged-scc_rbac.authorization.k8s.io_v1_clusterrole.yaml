# ClusterRole granting "use" on OpenShift's built-in privileged SCC so sbr-agent
# pods can run with required privileges (watchdog, block devices) without a custom SCC.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: sbr-operator-sbr-agent-privileged-scc
  labels:
    app.kubernetes.io/name: sbr-operator
    app.kubernetes.io/component: sbr-agent
rules:
- apiGroups:
  - security.openshift.io
  resources:
  - securitycontextconstraints
  resourceNames:
  - privileged
  verbs:
  - use
